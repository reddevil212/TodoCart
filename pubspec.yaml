name: todocart
description: "A simple for noW Advanced Todo Manager."
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  change_app_package_name: ^1.4.0
  provider: ^6.0.5
  url_launcher: ^6.3.1
  cupertino_icons: ^1.0.8
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

